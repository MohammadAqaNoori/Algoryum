# 🚀 Quick Start Guide

## Step 1: Install Dependencies

Open your terminal in the Algoryum folder and run:

```bash
npm install
```

This will install:
- React & React DOM
- React Router DOM
- Vite
- Tailwind CSS
- PostCSS & Autoprefixer

## Step 2: Start Development Server

```bash
npm run dev
```

You should see output like:
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

## Step 3: Open in Browser

Visit: **http://localhost:5173**

## 📄 Available Routes

- `/` - Landing Page
- `/home` - Home Page
- `/mission` - Mission & About
- `/process` - Process Workflow
- `/services` - Services Overview
- `/connect` - Contact Page

## 🎨 What You'll See

✅ Fully responsive design  
✅ Dark mode support  
✅ Smooth navigation  
✅ Material icons  
✅ Professional UI/UX  

## 🔧 Common Issues

### Port Already in Use
If port 5173 is busy, Vite will use the next available port automatically.

### Module Not Found
Make sure you ran `npm install` first.

### Styling Not Loading
Do a hard refresh: `Ctrl+Shift+R` (Windows) or `Cmd+Shift+R` (Mac)

## 📦 Build for Production

When ready to deploy:

```bash
npm run build
```

This creates an optimized build in the `dist/` folder.

## 🎯 Next Steps

1. Customize content in `src/pages/*.jsx`
2. Update colors in `tailwind.config.js`
3. Add your own images
4. Connect contact form to backend
5. Deploy to Vercel, Netlify, or your preferred host

---

**All 6 HTML pages successfully converted to React!** 🎉
